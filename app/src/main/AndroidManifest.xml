<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codepath.simpletodo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/SimpleTodoTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
                    <meta-data android:name="DATABASE_NAME" android:value="todosDatabase.db" />
                <meta-data android:name="DATABASE_VERSION" android:value="2" />
                <meta-data android:name="QUERY_LOG" android:value="true" />
                <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.codepath.simpletodo"/>
        -->

        <!--        <activity
                    android:name=".EmailTodoActivity"
                    android:theme="@android:style/Theme.Holo.Dialog" />-->
        <activity android:name=".EmailTodoActivity" />
    </application>

</manifest>